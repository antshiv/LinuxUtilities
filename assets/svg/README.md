# SVG Assets

This folder contains starter SVG assets for the screenshot editor UI and annotations.

- `callouts/`: reusable callout/box shapes.
- `icons/`: tool icons that can be loaded into GTK buttons or a quick-style panel.

These are intentionally simple so you can tweak stroke, color, opacity, and corner radius quickly.
